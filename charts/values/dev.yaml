# Default values for helm-charts.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
image:
  repository: registry.gitlab.com/lyra-egnify/hydra-sso/development
  tag: latest
  pullPolicy: Always
  pullSecrets: gitlab-registry-pull-secret

projectEnv:
  nodeEnv: "production"
  mongodbUri: "mongodb://hydra:AkdjsAIYHjk87y23l1m2klRrtyhjnmrti92w33@35.200.234.132:27017/tenantregistry-sso"
  svcSettings: "http://settings-dev-sso.dev.svc.cluster.local"
  svcTest: "http://test-management-dev-sso.dev.svc.cluster.local"
  jwtSecret: "KAJDK656kllksklfmFGDFGS765ujh765T6GHAJDSjjjkjiursrda"
  mailjetApiKey: "a1fb4988b5a359099061d2cb8aebc80d"
  mailjetApiSecretkey: "2fc962b7e3aae82acee17b355fb9de2d"
  mailjetApiSecretkey: "2fc962b7e3aae82acee17b355fb9de2d"
  apiEgnifyIoUrl: "https://api.dev.sso.egnify.io"
  curxParserUrl: "https://crux-parser-v1.egnify.com"

service:
  name: hydra-sso
  tier: backend
  type: ClusterIP
  externalPort: 80
  internalPort: 3000
ingress:
  enabled: true
  # Used to create an Ingress record.
  hosts:
    - accounts.dev.hydra.egnify.io
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "40M"
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: "true"
  tls:
    # Secrets must be manually created in the namespace.
    - secretName: hydra-sso-tls-dev
      hosts:
        - accounts.dev.hydra.egnify.io
resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #  cpu: 100m
  #  memory: 128Mi
  # requests:
  #  cpu: 100m
  #  memory: 128Mi
